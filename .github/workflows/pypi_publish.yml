# This file is autogenerated by maturin v0.14.17
# To update, run
#
#    maturin generate-ci github -o .github/workflows/pypi_publish.yml
#
name: OG-CICD

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  OG-CICD:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check out the code
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print message
      run: |
        echo "Original CI/CD ran"
    
